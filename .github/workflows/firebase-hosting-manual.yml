# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy manually to Firebase Hosting live
'on':
  workflow_dispatch:
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: curl https://www.npmjs.com/install.sh | bash && npm run install-dev && CI="" npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SPORT_LEGENDS_NFT }}'
          channelId: live
          projectId: sport-legends-nft
